# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11 with BiFPN neck for enhanced multi-scale fusion

# Parameters
nc: 80
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]
fusion_mode: bifpn
node_mode: C3k2
head_channel: 256  # 控制头部通道数的统一参数

# Backbone (保持不变)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]] # 2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]] # 4
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]] # 6
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2_EMSCP, [1024, True]] # 8
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head with BiFPN
head:
  # 第一阶段：特征通道统一
  - [3, 1, Conv, [head_channel]]  # 11-P3/8 (从backbone第3层获取)
  - [5, 1, Conv, [head_channel]]  # 12-P4/16 (从backbone第5层获取)
  - [10, 1, Conv, [head_channel]] # 13-P5/32 (从backbone第10层获取)

  # 第二阶段：BiFPN上采样路径
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 14 P5->P4
  - [[-1, 12], 1, Fusion, [fusion_mode]] # 15 融合P5上采样+P4
  - [-1, 3, C3k2, [head_channel, False]] # 16-P4/16

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 17 P4->P3
  - [[-1, 11], 1, Fusion, [fusion_mode]] # 18 融合P4上采样+P3
  - [-1, 3, C3k2, [head_channel, False]] # 19-P3/8

  # 第三阶段：BiFPN下采样路径
  - [19, 1, Conv, [head_channel, 3, 2]] # 20 P3->P4
  - [[-1, 16], 1, Fusion, [fusion_mode]] # 21 融合P3下采样+P4
  - [-1, 3, C3k2, [head_channel, False]] # 22-P4/16

  - [-1, 1, Conv, [head_channel, 3, 2]] # 23 P4->P5
  - [[-1, 13], 1, Fusion, [fusion_mode]] # 24 融合P4下采样+P5
  - [-1, 3, C3k2, [head_channel, True]] # 25-P5/32

  # 检测头
  - [[19, 22, 25], 1, Detect_LSCD, [nc, 256]] # Detect(P3, P4, P5)